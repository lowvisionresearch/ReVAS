for each frame in numberOfFrames:
    replacementPixels = randn(stimulusSize) * standardDeviationOfEachFrame[frame] + meanOfEachFrame[frame];
    location = stimulusLocationInEachFrame[frame];
    inputVideo[location[y] - stimulusSize[y],
        location[x] - stimulusSize[x],
        frame]
        = replacementPixels;
