scaledDownInputVideoFrames = scale_down(inputVideoFrames);
initialReferenceFrame = scaledDownFrame[median];

usefulEyePositionTraces = StripAnalysis(scaledDownInputVideoFrames, initialReferenceFrame);
usefulEyePositionTraces = scale_up(usefulEyePositionTraces);

cumulativePixelValues = [][];
counter = [][];

for each frame in numberOfFrames:
    # Add frame pixel values on to cumulativePixelValues according to its location
    # given by usefulEyePositionTraces.
    cumulativePixelValues[usefulEyePositionTraces[frame]] += frame;

    # Increment counter for pixels affected.
    counter[usefulEyePositionTraces[frame]] += 1;

coarseReferenceFrame = cumulativePixelValues / counter;
